{
  "name": "moniepoint-clone",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@google/genai": "^1.38.0"
  },
  "scripts": {
    "build": "echo 'No frontend build required - using Babel standalone'"
  }
}